name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.asset_name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest       # arm64 runner
            target: aarch64-apple-darwin
            asset_name: scout-macos-arm64
            binary: scout

          - os: macos-13           # x86_64 runner
            target: x86_64-apple-darwin
            asset_name: scout-macos-x64
            binary: scout

          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            asset_name: scout-linux-x64
            binary: scout

          - os: windows-latest
            target: x86_64-pc-windows-msvc
            asset_name: scout-windows-x64.exe
            binary: scout.exe

    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Build
        run: cargo build --release --target ${{ matrix.target }}

      - name: Stage binary (Unix)
        if: runner.os != 'Windows'
        run: cp target/${{ matrix.target }}/release/${{ matrix.binary }} ${{ matrix.asset_name }}

      - name: Stage binary (Windows)
        if: runner.os == 'Windows'
        run: cp target\${{ matrix.target }}\release\${{ matrix.binary }} ${{ matrix.asset_name }}

      - name: Upload to release
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ matrix.asset_name }}
          generate_release_notes: true
